# Clé secrète JWT — à changer obligatoirement en production
JWT_SECRET=changez-cette-cle-secrete-en-production

# Port du serveur (optionnel, défaut: 3000)
PORT=3000

# Environnement
NODE_ENV=production
